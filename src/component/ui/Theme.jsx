import { createTheme } from '@mui/material';

const Theme = createTheme({
    palette: {
        type: 'light',
        primary: {
            main: '#F3AA3C',
        },
        secondary: {
            main: '#E55A8B',
        },
    },
});

export default Theme
